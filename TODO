Treat every line as an independent instruction
in header "jason's oil rewards" is appearing twice.
On home, the drip icon needs some space between it and "Log Service"
Change "Log Service" to Service Log.
On customer Home button should just be back arrow.
Oppisite (rigth) the home button add a "Log Service" button that calls the function and refreshs/queries list
Center "Customer <number>".
Instead of the banner that appears when the history is valid for free oil change redemption just add an attention grabbing button that says "Redeem free Oil Change". all related relationships between logging service and redeem button must remain for the idenitification of which service is the redeemer.